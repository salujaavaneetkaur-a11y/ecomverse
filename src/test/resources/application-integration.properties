# ============================================================
# INTEGRATION TEST CONFIGURATION
# ============================================================
# This profile is used with TestContainers
# Database connection is dynamically injected by TestContainers
# ============================================================

spring.application.name=ecomverse-integration-test

# JPA Settings
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT Settings (for auth tests)
spring.app.jwtSecret=integrationTestSecretKey123912738aopsgjnspkmndfsopkvajoirjg94gf2opfng2moknm
spring.app.jwtExpirationMs=3600000
spring.ecom.app.jwtCookieName=springBootEcomIntegrationTest

# Image upload directory
project.image=test-images/

# Logging - show SQL for debugging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.com.ecommerce.project=DEBUG
